import { ThemeProvider } from '@mui/material/styles';

export default function BusinessLanding() {
    return(
        <ThemeProvider>
            <h1>This is the business settings page</h1>
        </ThemeProvider>
    );
}